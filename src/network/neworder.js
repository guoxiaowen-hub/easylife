export class Order {
  constructor(querry) {
    this.shopName = querry.shopName
    this.shopLogo = querry.shopLogo
    this.shopId = querry.shopId
    this.price = querry.price
    this.goodsName = querry.goodsName
    this.goodsImg = querry.goodsImg
    this.goodsId = querry.goodsId
    this.num = querry.num
  }
}
